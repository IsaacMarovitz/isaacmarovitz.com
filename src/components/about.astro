---
import { Image } from 'astro:assets';
import Godot from '../images/godot.svg';
---
<div class="flex justify-center px-5 sm:px-10">
  <div class="flex flex-col md:flex-row items-center justify-center max-w-[1300px] w-[100%] rounded-2xl border border-secondary/20 p-6 my-4">
    <div class="flex flex-col h-full">
      <h1 class="text-left font-bold lg:text-4xl md:text-4xl text-3xl pb-4 text-accent">About</h1>
      <p class="text-sm sm:text-base max-w-2xl">
        Hi there, I'm Isaac. I'm a <span id="age"></span> year old programmer from London.<br/><br/>
        Computer science has been my number one interest for over ten years.
        I started with web development and moved on to game development for 4 years.
        I then became involved in open-source app development.
        <br><br>
        I'm proficient in Swift, Rust, and C#, and have extensive experience with cross-platform computer graphics infrastructure, including Metal, Vulkan, and DirectX.
      </p>
    </div>
    <div class="grow"></div>
    <div class="hidden md:grid grid-cols-3">
      <div class="icon">
        <i class="fa-brands fa-rust"></i>
      </div>
      <div class="icon">
        <i class="fa-brands fa-swift"></i>
      </div>
      <div class="icon">
        <Image class="w-10 md:w-12 lg:w-16" src={Godot} alt="Godot logo"/>
      </div>
      <div class="icon">
        <p class="fa">C#</p>
      </div>
      <div class="icon">
        <i class="fa-brands fa-python"></i>
      </div>
      <div class="icon">
        <i class="fa-brands fa-html5"></i>
      </div>
    </div>
  </div>
</div>

<script>
  var birthday = new Date(2006, 5, 16);
  var today = new Date();
  var age = today.getFullYear() - birthday.getFullYear();
  var m = today.getMonth() - birthday.getMonth();
  if (m < 0 || (m === 0 && today.getDate() < birthday.getDate())) {
    age--;
  }
  var span = document.getElementById('age');
  if (span != null) {
    span.textContent = age.toString();
  }
</script>

<style>
  @reference "tailwindcss";
  .icon {
    @apply w-20 h-20 lg:w-30 lg:h-30;
    @apply flex items-center justify-center;
    @apply rounded-full;
    @apply text-4xl md:text-5xl lg:text-6xl;
    @apply select-none;
  }
  
  .icon p {
    @apply text-xl md:text-3xl lg:text-4xl;
  }
</style>