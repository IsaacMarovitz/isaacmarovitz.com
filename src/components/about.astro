---
import { Image } from 'astro:assets';
import Godot from '../images/godot.svg';
---

<div class="bg-secondary flex justify-center text-blue">
    <div class="flex flex-col md:flex-row items-center justify-start m-10 max-w-[1300px] w-[100%]">
        <div class="flex flex-col">
            <h1 class="text-left font-bold lg:text-4xl md:text-4xl text-3xl pb-0 lg:pb-1 md:pb-1">About</h1>
            <p class="text-sm sm:text-base max-w-2xl">
                Hi there, I'm Isaac. I'm a <span id="age"></span> year old programmer from London.<br/><br/>
                Computer science has been my number one interest for over ten years.
                I started with web development and moved on to game development for 4 years.
                I then became involved in open-source app development.

                <br><br>

                I'm proficient in Swift, Rust, and C#, and have extensive experience with cross-platform computer graphics infrastructure, including Metal, Vulkan, and DirectX.
            </p>
        </div>
        <div class="grow"></div>
        <div class="flex-col gap-10 hidden md:flex">
            <div class="flex flex-row gap-5 md:gap-10">
                <div class="flex">
                    <div class="icon">
                        <i class="text-4xl md:text-5xl lg:text-6xl fa-brands fa-rust"></i>
                    </div>
                </div>
                <div class="flex">
                    <div class="icon">
                        <i class="text-4xl md:text-5xl lg:text-6xl fa-brands fa-swift"></i>
                    </div>
                </div>
                <div class="flex">
                    <div class="icon">
                        <Image class="lg:p-5 p-2 select-none block" src={Godot} alt="Godot logo"/>
                    </div>
                </div>
            </div>
            <div class="flex flex-row gap-5 md:gap-10">
                <div class="flex">
                    <div class="icon">
                        <p class="fa text-xl md:text-3xl lg:text-4xl select-none">C#</p>
                    </div>
                </div>
                <div class="flex">
                    <div class="icon">
                        <i class="text-4xl md:text-5xl lg:text-6xl fa-brands fa-python"></i>
                    </div>
                </div>
                <div class="flex">
                    <div class="icon">
                        <i class="text-4xl md:text-5xl lg:text-6xl fa-brands fa-html5"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    var birthday = new Date(2006, 5, 16);
    var today = new Date();
    var age = today.getFullYear() - birthday.getFullYear();
    var m = today.getMonth() - birthday.getMonth();
    
    if (m < 0 || (m === 0 && today.getDate() < birthday.getDate())) {
        age--;
    }

    var span = document.getElementById('age');

    if (span != null) {
        span.textContent = age.toString();
    }
</script>

<style>
    @reference "tailwindcss";
    
    .icon {
        @apply max-w-20;
        @apply min-w-20;
        @apply max-h-20;
        @apply min-h-20;
        @apply lg:max-w-30;
        @apply lg:min-w-30;
        @apply lg:max-h-30;
        @apply lg:min-h-30;
        @apply flex;
        @apply items-center;
        @apply justify-center;
        @apply rounded-full;
    }
</style>
